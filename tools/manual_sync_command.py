def register_tool(app):\n    import requests, json\n    @app.route('/tools/manual_yggdrasil_sync', methods=['GET'])\n    def manual_sync():\n        try:\n            url = 'https://docgpt-memory-default-rtdb.firebaseio.com.json'  # <- Replace with your real Firebase URL\n            r = requests.get(url)\n            if r.status_code == 200:\n                with open('memory.json', 'w') as f:\n                    json.dump(r.json(), f, indent=2)\n                return {'status': 'Synced from Yggdrasil'}\n            else:\n                return {'error': 'Failed to fetch', 'code': r.status_code}\n        except Exception as e:\n            return {'error': str(e)}