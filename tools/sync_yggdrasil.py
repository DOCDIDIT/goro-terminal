def register_tool(app):\n    import requests, json\n    @app.route('/tools/sync_yggdrasil', methods=['GET'])\n    def sync_memory():\n        url = 'https://docgpt-memory-default-rtdb.firebaseio.com.json'  # Replace this\n        r = requests.get(url)\n        if r.status_code == 200:\n            with open('memory.json', 'w') as f:\n                json.dump(r.json(), f, indent=2)\n            return {'status': 'Memory synced from Yggdrasil'}\n        else:\n            return {'error': 'Failed to fetch from Yggdrasil', 'status_code': r.status_code}